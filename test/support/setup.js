"use strict";
let chai = require("chai"),
    expect = chai.expect,
    should = chai.should(),
    assert = chai.assert;
const chaiAsPromised = require("chai-as-promised");

chai.use(chaiAsPromised);
